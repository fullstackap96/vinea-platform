/**
 * Standard section title + subtle divider (use on `h2` in cards and dashboard sections).
 */
export const sectionHeadingClassName =
  'mb-5 border-b border-gray-200/80 pb-3 text-xl font-semibold tracking-tight text-gray-900'

/** Title text when paired with `sectionHeadingRowClassName` (divider spans full row). */
export const sectionHeadingTitleClassName =
  'text-xl font-semibold tracking-tight text-gray-900 sm:text-2xl'

/**
 * Full-width divider under a title row that may include trailing actions.
 */
export const sectionHeadingRowClassName =
  'mb-5 flex flex-col gap-3 border-b border-gray-200/80 pb-4 sm:flex-row sm:items-center sm:justify-between'

/** Nested block title (timeline, tool sub-panels). */
export const sectionSubheadingClassName =
  'mb-3 border-b border-gray-200/70 pb-2 text-base font-semibold tracking-tight text-gray-900 sm:text-lg'

/** Card section title (no extra bottom rule — wrap in a header with border if needed). */
export const sectionCardTitleClassName =
  'text-lg font-semibold tracking-tight text-gray-900 sm:text-xl'

/** Muted description under a card title. */
export const sectionCardDescriptionClassName =
  'mt-2 max-w-2xl text-base leading-relaxed text-gray-600 sm:text-[0.95rem]'
