/**
 * Tailwind classes appended to dashboard request cards/rows when
 * `next_follow_up_date` is overdue (Needs Attention, Follow-Up Queue, main list).
 * Keeps the red left stripe and adds a very subtle rose wash—no icons or motion.
 */
export const dashboardOverdueFollowUpCardClasses =
  'border-l-4 border-l-red-600 bg-gradient-to-r from-rose-100/35 via-rose-50/25 to-white'
